#include <fmt/core.h>
#include <bitset>
#include <vector>
#include <map>
#include <thread>
#include <iostream>
#include <sstream>
#include <fstream>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

//define max_size for packet
//__?__

//declare a structure packet_t with
// type -> int 
// age -> int 

//__?__

int main() {
    //Create array in heap which contains 10 packet_t
    // __?__
    //create a packet p2 which is the second packet of packets
    // __?__
    //how to replace the line above more simply? 
    // __?__
    //printf("packet = %p , p2 = %p, sizeof(packet_t) = %lu \n", packets, p2, sizeof(packet_t));
    //free(packets);
}